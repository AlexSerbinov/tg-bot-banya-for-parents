# Налаштування шрифтів

## Поточний шрифт

Бот використовує **Georgia** - системний serif шрифт з елегантним виглядом.

### Чому Georgia?

1. **Гарантована наявність** - системний шрифт macOS, не потребує завантаження
2. **Повна підтримка кирилиці** - коректно відображає українську мову
3. **Елегантний serif** - класичний стиль з високою читабельністю
4. **Надійність** - працює з коробки без додаткових налаштувань

## Історія змін шрифтів

### Спроби використання кастомних шрифтів

#### 1. Cormorant Garamond
- **Проблема**: Не підтримує кириличні символи
- **Результат**: Текст українською мовою не відображався

#### 2. Playfair Display
- **Проблема**: Незважаючи на заявлену підтримку кирилиці, Canvas не відображав українські літери
- **Результат**: Fallback на системний шрифт

#### 3. Georgia (фінальне рішення)
- **Рішення**: Використання системного шрифту Georgia
- **Результат**: Повністю робочий, елегантний вигляд

## Технічна реалізація

### Реєстрація шрифтів

```typescript
// src/core/scheduleImage.ts

// Функція для реєстрації кастомних шрифтів (якщо потрібно в майбутньому)
let fontsRegistered = false;
function registerCustomFonts() {
  if (fontsRegistered) return;

  const fontsDir = join(process.cwd(), 'fonts');
  try {
    GlobalFonts.registerFromPath(join(fontsDir, 'CustomFont.ttf'), 'Custom Font');
    console.log('✅ Custom fonts registered from:', fontsDir);
    fontsRegistered = true;
  } catch (error) {
    console.error('❌ Failed to register fonts from:', fontsDir, error);
  }
}
```

### Використання шрифтів

Всі тексти на зображенні використовують Georgia з різними вагами:

```typescript
// Заголовки
ctx.font = '700 48px Georgia';

// Підзаголовки
ctx.font = '600 28px Georgia';

// Звичайний текст
ctx.font = '500 22px Georgia';

// Текст на слотах (динамічний розмір)
ctx.font = `${weight} ${size}px Georgia`;
```

## Колір тексту

Всі плашки використовують білий текст `#f8fafc` для кращої читабельності на кольорових фонах:

- 🟢 Зелена плашка (баня без чану) - білий текст
- 🔵 Блакитна плашка (баня + чан) - білий текст
- 🔴 Червона плашка (зайнято) - білий текст

## Як змінити шрифт в майбутньому

### 1. Системний шрифт (рекомендовано)

Просто замінити `Georgia` на інший системний шрифт у файлі `src/core/scheduleImage.ts`:

```bash
sed -i '' 's/Georgia/Times New Roman/g' src/core/scheduleImage.ts
```

### 2. Кастомний шрифт

1. Завантажити .ttf файли шрифту в папку `fonts/`
2. Зареєструвати шрифт у функції `registerCustomFonts()`
3. Замінити всі входження назви шрифту в коді
4. **Важливо**: Переконатися, що шрифт підтримує кирилицю!

### Перевірка підтримки кирилиці

Після реєстрації шрифту перевірте логи:

```bash
pm2 logs simple-availability-bot | grep "font"
```

Має бути:
```
✅ [Font Name] fonts registered from: /path/to/fonts
🔍 [Font Name] found: true
```

Згенеруйте тестове зображення і перевірте, чи відображається український текст.

## Troubleshooting

### Шрифт не змінюється

1. Перевірте, що файли шрифтів знаходяться в папці `fonts/`
2. Перебілдіть проект: `npm run build`
3. Повністю перезапустіть бот: `pm2 restart simple-availability-bot`
4. Очистіть кеш: `rm -rf dist && npm run build`

### Кириличні символи не відображаються

1. Перевірте підтримку кирилиці в файлі шрифту
2. Спробуйте використати системний шрифт (Georgia, Times New Roman)
3. Перевірте логи на наявність помилок реєстрації шрифтів

### Текст не читабельний

1. Перевірте контраст тексту та фону
2. Змініть колір тексту в `drawSlotLabel()`:
   ```typescript
   ctx.fillStyle = '#ffffff'; // білий
   // або
   ctx.fillStyle = '#000000'; // чорний
   ```

## Файли для роботи зі шрифтами

- `src/core/scheduleImage.ts` - основний файл генерації зображення
- `fonts/` - папка з кастомними шрифтами (якщо використовуються)
- Всі входження шрифту можна знайти: `grep -n "font" src/core/scheduleImage.ts`

## Рекомендації

1. **Використовуйте системні шрифти** для надійності
2. **Завжди тестуйте** з українським текстом
3. **Перевіряйте читабельність** на різних фонах
4. **Документуйте зміни** в цьому файлі
