## Simple Availability Bot

Спрощений Telegram-бот для бані, який працює паралельно зі старим проєктом у власній папці. У ньому є лише два прості сценарії:

- **Адміністратор (тато)** додає вільні слоти за допомогою великих кнопок та покрокових підказок.
- **Клієнт** миттєво отримує актуальну картинку розкладу і текстову підказку, як записатися.

Візуальна частина робить акцент на чітких підписах і великих шрифтах, а вся інша інфраструктура (package.json, data, build) ізольована в цій новій папці.

---

### Можливості

- 📱 Перемикач режимів: дві великі кнопки миттєво показують або клієнтське меню (інформація + контакти), або панель адміністратора.
- ⏱ Майстер додавання: тепер обираємо день → початок → кінець, а бот сам зливає перетини й розширює вікна.
- 🗑 Очищення дня: швидкий вибір дати для прибирання застарілих слотів.
- 🖼 Генерація картинки розкладу на основі @napi-rs/canvas з великими кольоровими блоками.
- 💾 Зберігання в `data/availability.json` без додаткової БД.

---

### Запуск

```bash
cd simple-availability-bot
cp .env.example .env              # заповніть токен та контактне повідомлення
npm install                       # (вже виконано, якщо потрібно – перевстановіть)
npm run dev                       # або npm run build && npm start
```

У `.env` обовʼязково задайте:

- `BOT_TOKEN` – **новий** токен (не коміть його в репозиторій).
- `ADMIN_IDS` – список Telegram ID адміністраторів, наприклад `123456789`.
- Інші значення (час роботи, кроки, повідомлення для клієнтів) можна міняти при потребі.

---

### Основні команди

| Команда / кнопка       | Для кого      | Опис |
|-----------------------|---------------|------|
| `/start`              | всі           | Визначає роль, показує меню та картинку розкладу. |
| `/schedule`, `🗓 Показати розклад` | всі | Надсилає PNG-розклад з виділенням вільних блоків. |
| `/summary`            | всі           | Короткий список найближчих вільних вікон текстом. |
| `/admin`, `Меню`      | адміністратор | Показує клавіатуру з великими винесеними кнопками. |
| `➕ Додати слот`       | адміністратор | Запускає покроковий майстер (день → час → тривалість). |
| `📋 Всі слоти`         | адміністратор | Текстовий список по днях. |
| `🧹 Очистити день`     | адміністратор | Інлайн-меню для очищення вибраної дати. |
| `ℹ️ Інформація`       | клієнт        | Відображає поточні тарифи, мінімальний час і правила. |
| `📞 Контакти`          | клієнт        | Номери власників для бронювання. |

---

### Структура папки

```
simple-availability-bot/
├── data/availability.json      # збережені вільні вікна
├── src/
│   ├── bot/                    # сцени, клавіатури, launch
│   ├── core/scheduleImage.ts   # генератор картинки
│   ├── services/               # логіка слотів
│   └── storage/                # JSON-сховище
└── README.md
```

---

💡 Якщо потрібно розширити функціонал (наприклад, підтвердження бронювання або синхронізація з основною базою), це можна робити окремо, не чіпаючи головний бот.
