# 🚀 Швидкий старт

## Проблема з запуском? (409 Conflict)

Якщо бачите помилку **"409: Conflict: terminated by other getUpdates request"**:

### Виправлення:

```bash
# 1. Скинути стан бота
node reset-bot.js

# 2. Зачекати 30 секунд
# (Telegram закриває попередню сесію)

# 3. Запустити бота
npm run dev
```

### Альтернативні рішення:

- **Закрийте всі вкладки з ботом** у Telegram Web/Desktop
- **Зачекайте 2 хвилини** - Telegram автоматично закриє попередню сесію
- Переконайтеся, що бот **не запущений на іншому пристрої**

## Перший запуск

```bash
# Все вже встановлено та налаштовано!
npm run dev
```

## Тестування бота

1. Відкрийте [@codex_webhook_bot](https://t.me/codex_webhook_bot)
2. Натисніть `/start`
3. Виберіть "📅 Переглянути вільні слоти"
4. **Ви побачите вільні слоти з 09:00 до 23:00 щодня!**

## Вхід як адміністратор

Надішліть боту:
```
/start 14031995
```

Або інші коди:
- `svitlana2025`
- `stanislav2025`

## Структура проекту

```
TMB-backend/
└── tgbot/              ← ВСЯ ЛОГІКА БОТА ТУТ
    ├── src/            ← Вихідний код
    ├── prisma/         ← База даних (SQLite)
    ├── .env            ← Токен та налаштування
    └── README.md       ← Повна документація
```

## Корисні команди

```bash
# Запустити бота
npm run dev

# Скинути бота (при помилці 409)
node reset-bot.js

# Відкрити базу даних
npx prisma studio

# Переініціалізувати базу
npm run prisma:migrate
npm run prisma:seed
```

## Що далі?

Читайте повну документацію в [README.md](./README.md)
