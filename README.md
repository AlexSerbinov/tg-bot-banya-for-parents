# 🔥 Telegram Бот для Бронювання Бані

Повнофункціональний бот для бронювання лазні з адміністративною панеллю, створений на TypeScript.

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
npm install
```

### 2. Налаштування

Файл `.env` вже створений з вашим токеном бота. Змініть контактні дані власників:

```env
PUBLIC_CONTACT_SVITLANA_NAME=Світлана
PUBLIC_CONTACT_SVITLANA_PHONE=+380XXXXXXXXX
PUBLIC_CONTACT_STANISLAV_NAME=Станіслав
PUBLIC_CONTACT_STANISLAV_PHONE=+380YYYYYYYYY
```

### 3. Ініціалізація бази даних

База даних вже ініціалізована! Якщо потрібно перезапустити:

```bash
npm run prisma:migrate
npm run prisma:seed
```

### 4. Запуск бота

#### ⚠️ ВАЖЛИВО: Помилка "409 Conflict"

Якщо бачите помилку:
```
409: Conflict: terminated by other getUpdates request
```

Це означає, що бот вже запущений в іншому місці. Виправлення:

**Спосіб 1: Скинути webhook (рекомендовано)**
```bash
node reset-bot.js
```

**Спосіб 2: Зачекати 1-2 хвилини**
Telegram автоматично закриє попередню сесію.

**Спосіб 3: Якщо бот відкритий у веб-версії Telegram**
Закрийте всі вкладки з ботом у браузері/Telegram Web.

#### Запуск

```bash
npm run dev
```

Ви побачите:
```
🚀 Starting Telegram Banya Bot...
📡 Starting with long polling...
✅ Bot started successfully!
👤 Bot username: @codex_webhook_bot
```

## 📱 Використання

### Для клієнтів

1. Відкрийте бота: [@codex_webhook_bot](https://t.me/codex_webhook_bot)
2. Натисніть `/start`
3. Оберіть "📅 Переглянути вільні слоти"
4. Виберіть дату, тривалість (2/3/4 години) та час
5. Підтвердіть заявку
6. Чекайте підтвердження від адміністратора

### Для адміністраторів

1. Надішліть `/start 14031995` (або інший код з ADMIN_CODES)
2. Відкриється адмін-панель:
   - **📋 Заявки (нові)** - підтвердження/відхилення заявок
   - **📊 Список бронювань** - перегляд активних записів
   - **⚙️ Налаштування** - налаштування роботи
   - **👤 Режим клієнта** - повернутися до звичайного режиму

#### Коди доступу адміністратора:
- `14031995`
- `svitlana2025`
- `stanislav2025`

## ⚙️ Налаштування

### Робочі години (в .env)

```env
WORKING_DAYS=1,2,3,4,5,6,7        # 1=Пн, 7=Нд
DAY_OPEN_TIME=09:00               # Початок робочого дня
DAY_CLOSE_TIME=23:00              # Кінець робочого дня (слоти до 21:00-23:00)
ALLOWED_DURATIONS=2,3,4           # Доступні тривалості (години)
CLEANING_BUFFER_MIN=0             # Буфер між сеансами (хвилини)
```

### Змінити налаштування в базі даних

```bash
npx prisma studio
```

Відкриється веб-інтерфейс для редагування даних.

## 📂 Структура проекту

```
tgbot/
├── src/
│   ├── bot/
│   │   ├── handlers/
│   │   │   ├── start.ts          # Початкові команди
│   │   │   ├── customer.ts       # Функції для клієнтів
│   │   │   └── admin.ts          # Функції для адміністраторів
│   │   ├── keyboards.ts          # Клавіатури та кнопки
│   │   └── middlewares.ts        # Аутентифікація та сесії
│   ├── core/
│   │   ├── time.ts               # Робота з датами/часом
│   │   ├── rules.ts              # Генерація слотів
│   │   └── notifications.ts      # Повідомлення
│   ├── db/
│   │   └── prismaClient.ts       # Підключення до БД
│   ├── config.ts                 # Конфігурація
│   └── index.ts                  # Точка входу
├── prisma/
│   ├── schema.prisma             # Схема бази даних
│   └── seed.ts                   # Початкові дані
├── .env                          # Змінні середовища
└── package.json
```

## 🎯 Функціонал

### ✅ Реалізовано (MVP)

- [x] Перегляд вільних слотів (з фільтрацією за датою/тривалістю)
- [x] Створення заявок на бронювання
- [x] Сповіщення адміністраторів про нові заявки
- [x] Підтвердження/відхилення заявок адміністраторами
- [x] Сповіщення клієнтів про рішення
- [x] Перегляд контактів власників
- [x] Список активних бронювань для адміністраторів
- [x] Автоматична генерація слотів з урахуванням:
  - Робочих годин (09:00-23:00, кінець сеансу до 21:00-23:00)
  - Тривалостей (2/3/4 години)
  - Існуючих бронювань
  - Буферу прибирання (налаштовується)

### 🔜 Майбутні покращення

- [ ] Ручне додавання бронювань адміністраторами (телефонні заявки)
- [ ] Редагування налаштувань через бота
- [ ] Скасування бронювань
- [ ] Нагадування клієнтам
- [ ] Експорт у календар (.ics)
- [ ] Telegram Mini App з візуальним календарем
- [ ] Передоплата/депозит

## 🛠 Корисні команди

```bash
# Розробка
npm run dev                    # Запуск з автоперезавантаженням

# База даних
npm run prisma:generate        # Згенерувати Prisma Client
npm run prisma:migrate         # Застосувати міграції
npm run prisma:seed            # Заповнити початковими даними
npx prisma studio              # Відкрити веб-інтерфейс БД

# Скинути бота
node reset-bot.js              # Видалити webhook та очистити стан

# Продакшн
npm run build                  # Зібрати проект
npm start                      # Запустити продакшн версію
```

## 🐛 Налагодження

### Бот не запускається (409 Conflict)

Виконайте:
```bash
node reset-bot.js
```

Зачекайте 30 секунд та спробуйте знову.

### Порожній список слотів

Перевірте налаштування в `.env`:
- `DAY_OPEN_TIME` та `DAY_CLOSE_TIME`
- `ALLOWED_DURATIONS`

Або відкрийте Prisma Studio:
```bash
npx prisma studio
```

Перейдіть до таблиці `Settings` та перевірте значення.

### Адмін не отримує сповіщення

Переконайтеся, що ви увійшли як адміністратор:
1. Надішліть `/start КОД` (наприклад, `/start 14031995`)
2. Перевірте, що з'явилося адмін-меню

## 📞 Підтримка

Якщо виникли питання:
- Перевірте логи бота в консолі
- Перевірте базу даних через `npx prisma studio`
- Переконайтеся, що `.env` налаштований правильно

## 📝 Ліцензія

MIT

---

**Важливо:** Цей бот створений для демонстрації. Для production використання додайте:
- Redis для сесій (замість in-memory)
- PostgreSQL замість SQLite
- Логування (Winston/Pino)
- Моніторинг (Sentry)
- Docker для розгортання
